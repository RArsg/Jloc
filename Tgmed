-- Brookhaven Mobile Anti-Freeze (Simplified)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local Player = Players.LocalPlayer

RunService.Heartbeat:Connect(function()
    local character = Player.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end
    
    -- حماية دائمة من التجميد
    humanoid.WalkSpeed = 16  -- ثابتة دائماً
    humanoid.JumpPower = 50  -- ثابتة دائماً
    
    -- إذا حدث تجميد، أصلحه فوراً
    if humanoid.WalkSpeed < 16 then
        humanoid.WalkSpeed = 16
        warn("🛡️ Anti-Freeze: Fixed slow speed")
    end
    
    -- منع الجلوس القسري
    if humanoid.Sit then
        humanoid.Jump = true
        humanoid.Sit = false
    end
end)

print("📱 Mobile Anti-Freeze Activated!")
print("Your movement speed is now locked and protected!")
